# Копия корневого правила монорепозитория: в поставочной ветке (docs/delivery.md)
# каталог инспектора становится корнем, и корневого .gitattributes рядом нет.
# Без него клон ветки на Windows получает CRLF.
* text=auto eol=lf
