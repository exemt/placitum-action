# Копия корневых правил: в поставочной ветке (docs/delivery.md) корневого
# .gitignore рядом не будет.

# go build в каталоге инспектора
/action
/inspector

# Применённое поколение локального запуска: WAF_ACTION_DATA по умолчанию --
# <каталог профилей>.applied
/profiles.applied/
